import mainActions from '@/redux-folder/actions/mainActions';
import userActions from '@/redux-folder/actions/userActions';

const allActions = {
  mainActions,
  userActions,
};

export default allActions;
